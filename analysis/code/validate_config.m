function validate_config(desired_config,existing_config_path)
disp('this function does not exist yet.')
%TODO: some params may be dependent on some of the initial params making it
%beneficial to ignore them when validating here
end