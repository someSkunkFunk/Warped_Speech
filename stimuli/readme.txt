may end up with large files here when generating stimuli with 
different warp parameters....
should decide if we want to configure temp folders in script that "fixes" paths
and/or just store all wav files on box regardless of development stage